---
import { LayerBadge as LayerBadgeReact } from '@virtuslab/visdom-ui';

interface Props {
  layer: 0 | 1 | 2 | 3;
  label?: string;
}

const { layer, label } = Astro.props;
---
<LayerBadgeReact layer={layer} label={label} />
